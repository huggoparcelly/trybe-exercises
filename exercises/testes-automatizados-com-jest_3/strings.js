const upperCase = (string) => string.toUpperCase();

const firstLetter = (string) => string[0];

const concatened = (string1, string2) => string1 + string2

module.exports = {
  upperCase, 
  firstLetter, 
  concatened,
}